# plusMinus
# brute force
# def plusMinus(arr):
    # pos,neg,zero = 0,0,0
    # for i in arr:
    #     if i > 0:
    #         pos += 1
    #     elif i < 0:
    #         neg += 1
    #     else: 
    #         zero += 1
    # res = [format(pos/len(arr), '6f'), format(neg/len(arr), '6f'), format(zero/len(arr), '6f')]
    # for i in res:
    #     print(i)
   
# orgainzed from github
def plusMinus(arr):
    return arr

arr = [-4,3,-9,0,4,1]
plusMinus(arr)